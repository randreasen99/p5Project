let virusX;
let virusY;

let virusXspeed = 3;
let virusYspeed = 3;

let victim1Xspeed = 3;
let victim1Yspeed = 3;
let victim2Xspeed = 3;
let victim2Yspeed = 3;
let victim3Xspeed = 3;
let victim3Yspeed = 3;
let victim4Xspeed = 3;
let victim4Yspeed = 3;
let victim5Xspeed = 3;
let victim5Yspeed = 3;
let victim6Xspeed = 3;
let victim6Yspeed = 3;
let victim7Xspeed = 3;
let victim7Yspeed = 3;
let victim8Xspeed = 3;
let victim8Yspeed = 3;
let victim9Xspeed = 3;
let victim9Yspeed = 3;
let victim10Xspeed = 3;
let victim10Yspeed = 3;
let victim11Xspeed = 3;
let victim11Yspeed = 3;
let victim12Xspeed = 3;
let victim12Yspeed = 3;
let victim13Xspeed = 3;
let victim13Yspeed = 3;
let victim14Xspeed = 3;
let victim14Yspeed = 3;
let victim15Xspeed = 3;
let victim15Yspeed = 3;
let victim16Xspeed = 3;
let victim16Yspeed = 3;
let victim17Xspeed = 3;
let victim17Yspeed = 3;
let victim18Xspeed = 3;
let victim18Yspeed = 3;
let victim19Xspeed = 3;
let victim19Yspeed = 3;
let victim20Xspeed = 3;
let victim20Yspeed = 3;
let victim21Xspeed = 3;
let victim21Yspeed = 3;
let victim22Xspeed = 3;
let victim22Yspeed = 3;
let victim23Xspeed = 3;
let victim23Yspeed = 3;
let victim24Xspeed = 3;
let victim24Yspeed = 3;
let victim25Xspeed = 3;
let victim25Yspeed = 3;
let victim26Xspeed = 3;
let victim26Yspeed = 3;
let victim27Xspeed = 3;
let victim27Yspeed = 3;
let victim28Xspeed = 3;
let victim28Yspeed = 3;
let victim29Xspeed = 3;
let victim29Yspeed = 3;
let victim30Xspeed = 3;
let victim30Yspeed = 3;
let victim31Xspeed = 3;
let victim31Yspeed = 3;
let victim32Xspeed = 3;
let victim32Yspeed = 3;
let victim33Xspeed = 3;
let victim33Yspeed = 3;
let victim34Xspeed = 3;
let victim34Yspeed = 3;
let victim35Xspeed = 3;
let victim35Yspeed = 3;
let victim36Xspeed = 3;
let victim36Yspeed = 3;
let victim37Xspeed = 3;
let victim37Yspeed = 3;
let victim38Xspeed = 3;
let victim38Yspeed = 3;
let victim39Xspeed = 3;
let victim39Yspeed = 3;
let victim40Xspeed = 3;
let victim40Yspeed = 3;
let victim41Xspeed = 3;
let victim41Yspeed = 3;
let victim42Xspeed = 3;
let victim42Yspeed = 3;
let victim43Xspeed = 3;
let victim43Yspeed = 3;
let victim44Xspeed = 3;
let victim44Yspeed = 3;
let victim45Xspeed = 3;
let victim45Yspeed = 3;
let victim46Xspeed = 3;
let victim46Yspeed = 3;
let victim47Xspeed = 3;
let victim47Yspeed = 3;
let victim48Xspeed = 3;
let victim48Yspeed = 3;
let victim49Xspeed = 3;
let victim49Yspeed = 3;
let victim50Xspeed = 3;
let victim50Yspeed = 3;


let virusDiameter = 200;


//victim variables and booleans
let victim1X;
let victim1Y;
let victim2X;
let victim2Y;
let victim3X;
let victim3Y;
let victim4X;
let victim4Y;
let victim5X;
let victim5Y;
let victim6X;
let victim6Y;
let victim7X;
let victim7Y;
let victim8X;
let victim8Y;
let victim9X;
let victim9Y;
let victim10X;
let victim10Y;
let victim11X;
let victim11Y;
let victim12X;
let victim12Y;
let victim13X;
let victim13Y;
let victim14X;
let victim14Y;
let victim15X;
let victim15Y;
let victim16X;
let victim16Y;
let victim17X;
let victim17Y;
let victim18X;
let victim18Y;
let victim19X;
let victim19Y;
let victim20X;
let victim20Y;
let victim21X;
let victim21Y;
let victim22X;
let victim22Y;
let victim23X;
let victim23Y;
let victim24X;
let victim24Y;
let victim25X;
let victim25Y;
let victim26X;
let victim26Y;
let victim27X;
let victim27Y;
let victim28X;
let victim28Y;
let victim29X;
let victim29Y;
let victim30X;
let victim30Y;
let victim31X;
let victim31Y;
let victim32X;
let victim32Y;
let victim33X;
let victim33Y;
let victim34X;
let victim34Y;
let victim35X;
let victim35Y;
let victim36X;
let victim36Y;
let victim37X;
let victim37Y;
let victim38X;
let victim38Y;
let victim39X;
let victim39Y;
let victim40X;
let victim40Y;
let victim41X;
let victim41Y;
let victim42X;
let victim42Y;
let victim43X;
let victim43Y;
let victim44X;
let victim44Y;
let victim45X;
let victim45Y;
let victim46X;
let victim46Y;
let victim47X;
let victim47Y;
let victim48X;
let victim48Y;
let victim49X;
let victim49Y;
let victim50X;
let victim50Y;


let victim1Bool = false;
let victim2Bool = false;
let victim3Bool = false;
let victim4Bool = false;
let victim5Bool = false;
let victim6Bool = false;
let victim7Bool = false;
let victim8Bool = false;
let victim9Bool = false;
let victim10Bool = false;
let victim11Bool = false;
let victim12Bool = false;
let victim13Bool = false;
let victim14Bool = false;
let victim15Bool = false;
let victim16Bool = false;
let victim17Bool = false;
let victim18Bool = false;
let victim19Bool = false;
let victim20Bool = false;
let victim21Bool = false;
let victim22Bool = false;
let victim23Bool = false;
let victim24Bool = false;
let victim25Bool = false;
let victim26Bool = false;
let victim27Bool = false;
let victim28Bool = false;
let victim29Bool = false;
let victim30Bool = false;
let victim31Bool = false;
let victim32Bool = false;
let victim33Bool = false;
let victim34Bool = false;
let victim35Bool = false;
let victim36Bool = false;
let victim37Bool = false;
let victim38Bool = false;
let victim39Bool = false;
let victim40Bool = false;
let victim41Bool = false;
let victim42Bool = false;
let victim43Bool = false;
let victim44Bool = false;
let victim45Bool = false;
let victim46Bool = false;
let victim47Bool = false;
let victim48Bool = false;
let victim49Bool = false;
let victim50Bool = false;


function setup(){
  createCanvas(windowWidth, windowHeight);
  virusX = random(100, windowWidth - 100);
  virusY = random(100, windowHeight - 100);

  victim1X= random(100, windowWidth - 100);
  victim1Y= random(100, windowHeight - 100);

  victim2X= random(100, windowWidth - 100);
  victim2Y= random(100, windowHeight - 100);

  victim3X= random(100, windowWidth - 100);
  victim3Y= random(100, windowHeight - 100);

  victim4X= random(100, windowWidth - 100);
  victim4Y= random(100, windowHeight - 100);

  victim5X= random(100, windowWidth - 100);
  victim5Y= random(100, windowHeight - 100);

  victim6X= random(100, windowWidth - 100);
  victim6Y= random(100, windowHeight - 100);

  victim7X= random(100, windowWidth - 100);
  victim7Y= random(100, windowHeight - 100);

  victim8X= random(100, windowWidth - 100);
  victim8Y= random(100, windowHeight - 100);

  victim9X= random(100, windowWidth - 100);
  victim9Y= random(100, windowHeight - 100);

  victim10X= random(100, windowWidth - 100);
  victim10Y= random(100, windowHeight - 100);

  victim11X= random(100, windowWidth - 100);
  victim11Y= random(100, windowHeight - 100);

  victim12X= random(100, windowWidth - 100);
  victim12Y= random(100, windowHeight - 100);

  victim13X= random(100, windowWidth - 100);
  victim13Y= random(100, windowHeight - 100);

  victim14X= random(100, windowWidth - 100);
  victim14Y= random(100, windowHeight - 100);

  victim15X= random(100, windowWidth - 100);
  victim15Y= random(100, windowHeight - 100);

  victim16X= random(100, windowWidth - 100);
  victim16Y= random(100, windowHeight - 100);

  victim17X= random(100, windowWidth - 100);
  victim17Y= random(100, windowHeight - 100);

  victim18X= random(100, windowWidth - 100);
  victim18Y= random(100, windowHeight - 100);

  victim19X= random(100, windowWidth - 100);
  victim19Y= random(100, windowHeight - 100);

  victim20X= random(100, windowWidth - 100);
  victim20Y= random(100, windowHeight - 100);

  victim21X= random(100, windowWidth - 100);
  victim21Y= random(100, windowHeight - 100);

  victim22X= random(100, windowWidth - 100);
  victim22Y= random(100, windowHeight - 100);

  victim23X= random(100, windowWidth - 100);
  victim23Y= random(100, windowHeight - 100);

  victim24X= random(100, windowWidth - 100);
  victim24Y= random(100, windowHeight - 100);

  victim25X= random(100, windowWidth - 100);
  victim25Y= random(100, windowHeight - 100);

  victim26X= random(100, windowWidth - 100);
  victim26Y= random(100, windowHeight - 100);

  victim27X= random(100, windowWidth - 100);
  victim27Y= random(100, windowHeight - 100);

  victim28X= random(100, windowWidth - 100);
  victim28Y= random(100, windowHeight - 100);

  victim29X= random(100, windowWidth - 100);
  victim29Y= random(100, windowHeight - 100);

  victim30X= random(100, windowWidth - 100);
  victim30Y= random(100, windowHeight - 100);

  victim31X= random(100, windowWidth - 100);
  victim31Y= random(100, windowHeight - 100);

  victim32X= random(100, windowWidth - 100);
  victim32Y= random(100, windowHeight - 100);

  victim33X= random(100, windowWidth - 100);
  victim33Y= random(100, windowHeight - 100);

  victim34X= random(100, windowWidth - 100);
  victim34Y= random(100, windowHeight - 100);

  victim35X= random(100, windowWidth - 100);
  victim35Y= random(100, windowHeight - 100);

  victim36X= random(100, windowWidth - 100);
  victim36Y= random(100, windowHeight - 100);

  victim37X= random(100, windowWidth - 100);
  victim37Y= random(100, windowHeight - 100);

  victim38X= random(100, windowWidth - 100);
  victim38Y= random(100, windowHeight - 100);

  victim39X= random(100, windowWidth - 100);
  victim39Y= random(100, windowHeight - 100);

  victim40X= random(100, windowWidth - 100);
  victim40Y= random(100, windowHeight - 100);

  victim41X= random(100, windowWidth - 100);
  victim41Y= random(100, windowHeight - 100);

  victim42X= random(100, windowWidth - 100);
  victim42Y= random(100, windowHeight - 100);

  victim43X= random(100, windowWidth - 100);
  victim43Y= random(100, windowHeight - 100);

  victim44X= random(100, windowWidth - 100);
  victim44Y= random(100, windowHeight - 100);

  victim45X= random(100, windowWidth - 100);
  victim45Y= random(100, windowHeight - 100);

  victim46X= random(100, windowWidth - 100);
  victim46Y= random(100, windowHeight - 100);

  victim47X= random(100, windowWidth - 100);
  victim47Y= random(100, windowHeight - 100);

  victim48X= random(100, windowWidth - 100);
  victim48Y= random(100, windowHeight - 100);

  victim49X= random(100, windowWidth - 100);
  victim49Y= random(100, windowHeight - 100);

  victim50X= random(100, windowWidth - 100);
  victim50Y= random(100, windowHeight - 100);
}


function draw(){
  background(0);
  noStroke();
  fill(0, 255, 0);

  //virus animation
  virusX = virusX + virusXspeed;
  virusY = virusY + virusYspeed;
  if(virusX >= windowWidth - 10 || virusX <= 10){
    virusXspeed = virusXspeed * -1;
  }
  ellipse(virusX, virusY, 20, 20);

  if(virusY >= windowHeight - 10 || virusY <= 10){
    virusYspeed = virusYspeed * -1;
  }


  //victim animations
  victim1X = victim1X + victim1Xspeed;
  victim1Y = victim1Y + victim1Yspeed;
  if(victim1X >= windowWidth - 10 || victim1X <= 10){
    victim1Xspeed = victim1Xspeed * -1;
  }
  if(victim1Y >= windowHeight - 10 || victim1Y <= 10){
    victim1Yspeed = victim1Yspeed * -1;
  }

  victim2X = victim2X + victim2Xspeed;
  victim2Y = victim2Y + victim2Yspeed;
  if(victim2X >= windowWidth - 10 || victim2X <= 10){
    victim2Xspeed = victim2Xspeed * -1;
  }
  if(victim2Y >= windowHeight - 10 || victim2Y <= 10){
    victim2Yspeed = victim2Yspeed * -1;
  }

  victim3X = victim3X + victim3Xspeed;
  victim3Y = victim3Y + victim3Yspeed;
  if(victim3X >= windowWidth - 10 || victim3X <= 10){
    victim3Xspeed = victim3Xspeed * -1;
  }
  if(victim3Y >= windowHeight - 10 || victim3Y <= 10){
    victim3Yspeed = victim3Yspeed * -1;
  }

  victim4X = victim4X + victim4Xspeed;
  victim4Y = victim4Y + victim4Yspeed;
  if(victim4X >= windowWidth - 10 || victim4X <= 10){
    victim4Xspeed = victim4Xspeed * -1;
  }
  if(victim4Y >= windowHeight - 10 || victim4Y <= 10){
    victim4Yspeed = victim4Yspeed * -1;
  }

  victim5X = victim5X + victim5Xspeed;
  victim5Y = victim5Y + victim5Yspeed;
  if(victim5X >= windowWidth - 10 || victim5X <= 10){
    victim5Xspeed = victim5Xspeed * -1;
  }
  if(victim5Y >= windowHeight - 10 || victim5Y <= 10){
    victim5Yspeed = victim5Yspeed * -1;
  }

  victim6X = victim6X + victim6Xspeed;
  victim6Y = victim6Y + victim6Yspeed;
  if(victim6X >= windowWidth - 10 || victim6X <= 10){
    victim6Xspeed = victim6Xspeed * -1;
  }
  if(victim6Y >= windowHeight - 10 || victim6Y <= 10){
    victim6Yspeed = victim6Yspeed * -1;
  }

  victim7X = victim7X + victim7Xspeed;
  victim7Y = victim7Y + victim7Yspeed;
  if(victim7X >= windowWidth - 10 || victim7X <= 10){
    victim7Xspeed = victim7Xspeed * -1;
  }
  if(victim7Y >= windowHeight - 10 || victim7Y <= 10){
    victim7Yspeed = victim7Yspeed * -1;
  }

  victim8X = victim8X + victim8Xspeed;
  victim8Y = victim8Y + victim8Yspeed;
  if(victim8X >= windowWidth - 10 || victim8X <= 10){
    victim8Xspeed = victim8Xspeed * -1;
  }
  if(victim8Y >= windowHeight - 10 || victim8Y <= 10){
    victim8Yspeed = victim8Yspeed * -1;
  }

  victim9X = victim9X + victim9Xspeed;
  victim9Y = victim9Y + victim9Yspeed;
  if(victim9X >= windowWidth - 10 || victim9X <= 10){
    victim9Xspeed = victim9Xspeed * -1;
  }
  if(victim9Y >= windowHeight - 10 || victim9Y <= 10){
    victim9Yspeed = victim9Yspeed * -1;
  }

  victim10X = victim10X + victim10Xspeed;
  victim10Y = victim10Y + victim10Yspeed;
  if(victim10X >= windowWidth - 10 || victim10X <= 10){
    victim10Xspeed = victim10Xspeed * -1;
  }
  if(victim10Y >= windowHeight - 10 || victim10Y <= 10){
    victim10Yspeed = victim10Yspeed * -1;
  }

  //victim animations
  victim11X = victim11X + victim11Xspeed;
  victim11Y = victim11Y + victim11Yspeed;
  if(victim11X >= windowWidth - 10 || victim11X <= 10){
    victim11Xspeed = victim11Xspeed * -1;
  }
  if(victim11Y >= windowHeight - 10 || victim11Y <= 10){
    victim11Yspeed = victim11Yspeed * -1;
  }

  victim12X = victim12X + victim12Xspeed;
  victim12Y = victim12Y + victim12Yspeed;
  if(victim12X >= windowWidth - 10 || victim12X <= 10){
    victim12Xspeed = victim12Xspeed * -1;
  }
  if(victim12Y >= windowHeight - 10 || victim12Y <= 10){
    victim12Yspeed = victim12Yspeed * -1;
  }

  victim13X = victim13X + victim13Xspeed;
  victim13Y = victim13Y + victim13Yspeed;
  if(victim13X >= windowWidth - 10 || victim13X <= 10){
    victim13Xspeed = victim13Xspeed * -1;
  }
  if(victim13Y >= windowHeight - 10 || victim13Y <= 10){
    victim13Yspeed = victim13Yspeed * -1;
  }

  victim14X = victim14X + victim14Xspeed;
  victim14Y = victim14Y + victim14Yspeed;
  if(victim14X >= windowWidth - 10 || victim14X <= 10){
    victim14Xspeed = victim14Xspeed * -1;
  }
  if(victim14Y >= windowHeight - 10 || victim14Y <= 10){
    victim14Yspeed = victim14Yspeed * -1;
  }

  victim15X = victim15X + victim15Xspeed;
  victim15Y = victim15Y + victim15Yspeed;
  if(victim15X >= windowWidth - 10 || victim15X <= 10){
    victim15Xspeed = victim15Xspeed * -1;
  }
  if(victim15Y >= windowHeight - 10 || victim15Y <= 10){
    victim15Yspeed = victim15Yspeed * -1;
  }

  victim16X = victim16X + victim16Xspeed;
  victim16Y = victim16Y + victim16Yspeed;
  if(victim16X >= windowWidth - 10 || victim16X <= 10){
    victim16Xspeed = victim16Xspeed * -1;
  }
  if(victim16Y >= windowHeight - 10 || victim16Y <= 10){
    victim16Yspeed = victim16Yspeed * -1;
  }

  victim17X = victim17X + victim17Xspeed;
  victim17Y = victim17Y + victim17Yspeed;
  if(victim17X >= windowWidth - 10 || victim17X <= 10){
    victim17Xspeed = victim17Xspeed * -1;
  }
  if(victim17Y >= windowHeight - 10 || victim17Y <= 10){
    victim17Yspeed = victim17Yspeed * -1;
  }

  victim18X = victim18X + victim18Xspeed;
  victim18Y = victim18Y + victim18Yspeed;
  if(victim18X >= windowWidth - 10 || victim18X <= 10){
    victim18Xspeed = victim18Xspeed * -1;
  }
  if(victim18Y >= windowHeight - 10 || victim18Y <= 10){
    victim18Yspeed = victim18Yspeed * -1;
  }

  victim19X = victim19X + victim19Xspeed;
  victim19Y = victim19Y + victim19Yspeed;
  if(victim19X >= windowWidth - 10 || victim19X <= 10){
    victim19Xspeed = victim19Xspeed * -1;
  }
  if(victim19Y >= windowHeight - 10 || victim19Y <= 10){
    victim19Yspeed = victim19Yspeed * -1;
  }

  victim20X = victim20X + victim20Xspeed;
  victim20Y = victim20Y + victim20Yspeed;
  if(victim20X >= windowWidth - 10 || victim20X <= 10){
    victim20Xspeed = victim20Xspeed * -1;
  }
  if(victim20Y >= windowHeight - 10 || victim20Y <= 10){
    victim20Yspeed = victim20Yspeed * -1;
  }

  //victim animations
  victim21X = victim21X + victim21Xspeed;
  victim21Y = victim21Y + victim21Yspeed;
  if(victim21X >= windowWidth - 10 || victim21X <= 10){
    victim21Xspeed = victim21Xspeed * -1;
  }
  if(victim21Y >= windowHeight - 10 || victim21Y <= 10){
    victim21Yspeed = victim21Yspeed * -1;
  }

  victim22X = victim22X + victim22Xspeed;
  victim22Y = victim22Y + victim22Yspeed;
  if(victim22X >= windowWidth - 10 || victim22X <= 10){
    victim22Xspeed = victim22Xspeed * -1;
  }
  if(victim22Y >= windowHeight - 10 || victim22Y <= 10){
    victim22Yspeed = victim22Yspeed * -1;
  }

  victim23X = victim23X + victim23Xspeed;
  victim23Y = victim23Y + victim23Yspeed;
  if(victim23X >= windowWidth - 10 || victim23X <= 10){
    victim23Xspeed = victim23Xspeed * -1;
  }
  if(victim23Y >= windowHeight - 10 || victim23Y <= 10){
    victim23Yspeed = victim23Yspeed * -1;
  }

  victim24X = victim24X + victim24Xspeed;
  victim24Y = victim24Y + victim24Yspeed;
  if(victim24X >= windowWidth - 10 || victim24X <= 10){
    victim24Xspeed = victim24Xspeed * -1;
  }
  if(victim24Y >= windowHeight - 10 || victim24Y <= 10){
    victim24Yspeed = victim24Yspeed * -1;
  }

  victim25X = victim25X + victim25Xspeed;
  victim25Y = victim25Y + victim25Yspeed;
  if(victim25X >= windowWidth - 10 || victim25X <= 10){
    victim25Xspeed = victim25Xspeed * -1;
  }
  if(victim25Y >= windowHeight - 10 || victim25Y <= 10){
    victim25Yspeed = victim25Yspeed * -1;
  }

  victim26X = victim26X + victim26Xspeed;
  victim26Y = victim26Y + victim26Yspeed;
  if(victim26X >= windowWidth - 10 || victim26X <= 10){
    victim26Xspeed = victim26Xspeed * -1;
  }
  if(victim26Y >= windowHeight - 10 || victim26Y <= 10){
    victim26Yspeed = victim26Yspeed * -1;
  }

  victim27X = victim27X + victim27Xspeed;
  victim27Y = victim27Y + victim27Yspeed;
  if(victim27X >= windowWidth - 10 || victim27X <= 10){
    victim27Xspeed = victim27Xspeed * -1;
  }
  if(victim27Y >= windowHeight - 10 || victim27Y <= 10){
    victim27Yspeed = victim27Yspeed * -1;
  }

  victim28X = victim28X + victim28Xspeed;
  victim28Y = victim28Y + victim28Yspeed;
  if(victim28X >= windowWidth - 10 || victim28X <= 10){
    victim28Xspeed = victim28Xspeed * -1;
  }
  if(victim28Y >= windowHeight - 10 || victim28Y <= 10){
    victim28Yspeed = victim28Yspeed * -1;
  }

  victim29X = victim29X + victim29Xspeed;
  victim29Y = victim29Y + victim29Yspeed;
  if(victim29X >= windowWidth - 10 || victim29X <= 10){
    victim29Xspeed = victim29Xspeed * -1;
  }
  if(victim29Y >= windowHeight - 10 || victim29Y <= 10){
    victim29Yspeed = victim29Yspeed * -1;
  }

  victim30X = victim30X + victim30Xspeed;
  victim30Y = victim30Y + victim30Yspeed;
  if(victim30X >= windowWidth - 10 || victim30X <= 10){
    victim30Xspeed = victim30Xspeed * -1;
  }
  if(victim30Y >= windowHeight - 10 || victim30Y <= 10){
    victim30Yspeed = victim30Yspeed * -1;
  }

  //victim animations
  victim31X = victim31X + victim31Xspeed;
  victim31Y = victim31Y + victim31Yspeed;
  if(victim31X >= windowWidth - 10 || victim31X <= 10){
    victim31Xspeed = victim31Xspeed * -1;
  }
  if(victim31Y >= windowHeight - 10 || victim31Y <= 10){
    victim31Yspeed = victim31Yspeed * -1;
  }

  victim32X = victim32X + victim32Xspeed;
  victim32Y = victim32Y + victim32Yspeed;
  if(victim32X >= windowWidth - 10 || victim32X <= 10){
    victim32Xspeed = victim32Xspeed * -1;
  }
  if(victim32Y >= windowHeight - 10 || victim32Y <= 10){
    victim32Yspeed = victim32Yspeed * -1;
  }

  victim33X = victim33X + victim33Xspeed;
  victim33Y = victim33Y + victim33Yspeed;
  if(victim33X >= windowWidth - 10 || victim33X <= 10){
    victim33Xspeed = victim33Xspeed * -1;
  }
  if(victim33Y >= windowHeight - 10 || victim33Y <= 10){
    victim33Yspeed = victim33Yspeed * -1;
  }

  victim34X = victim34X + victim34Xspeed;
  victim34Y = victim34Y + victim34Yspeed;
  if(victim34X >= windowWidth - 10 || victim34X <= 10){
    victim34Xspeed = victim34Xspeed * -1;
  }
  if(victim34Y >= windowHeight - 10 || victim34Y <= 10){
    victim34Yspeed = victim34Yspeed * -1;
  }

  victim35X = victim35X + victim35Xspeed;
  victim35Y = victim35Y + victim35Yspeed;
  if(victim35X >= windowWidth - 10 || victim35X <= 10){
    victim35Xspeed = victim35Xspeed * -1;
  }
  if(victim35Y >= windowHeight - 10 || victim35Y <= 10){
    victim35Yspeed = victim35Yspeed * -1;
  }

  victim36X = victim36X + victim36Xspeed;
  victim36Y = victim36Y + victim36Yspeed;
  if(victim36X >= windowWidth - 10 || victim36X <= 10){
    victim36Xspeed = victim36Xspeed * -1;
  }
  if(victim36Y >= windowHeight - 10 || victim36Y <= 10){
    victim36Yspeed = victim36Yspeed * -1;
  }

  victim37X = victim37X + victim37Xspeed;
  victim37Y = victim37Y + victim37Yspeed;
  if(victim37X >= windowWidth - 10 || victim37X <= 10){
    victim37Xspeed = victim37Xspeed * -1;
  }
  if(victim37Y >= windowHeight - 10 || victim37Y <= 10){
    victim37Yspeed = victim37Yspeed * -1;
  }

  victim38X = victim38X + victim38Xspeed;
  victim38Y = victim38Y + victim38Yspeed;
  if(victim38X >= windowWidth - 10 || victim38X <= 10){
    victim38Xspeed = victim38Xspeed * -1;
  }
  if(victim38Y >= windowHeight - 10 || victim38Y <= 10){
    victim38Yspeed = victim38Yspeed * -1;
  }

  victim39X = victim39X + victim39Xspeed;
  victim39Y = victim39Y + victim39Yspeed;
  if(victim39X >= windowWidth - 10 || victim39X <= 10){
    victim39Xspeed = victim39Xspeed * -1;
  }
  if(victim39Y >= windowHeight - 10 || victim39Y <= 10){
    victim39Yspeed = victim39Yspeed * -1;
  }

  victim40X = victim40X + victim40Xspeed;
  victim40Y = victim40Y + victim40Yspeed;
  if(victim40X >= windowWidth - 10 || victim40X <= 10){
    victim40Xspeed = victim40Xspeed * -1;
  }
  if(victim40Y >= windowHeight - 10 || victim40Y <= 10){
    victim40Yspeed = victim40Yspeed * -1;
  }

  //victim animations
  victim41X = victim41X + victim41Xspeed;
  victim41Y = victim41Y + victim41Yspeed;
  if(victim41X >= windowWidth - 10 || victim41X <= 10){
    victim41Xspeed = victim41Xspeed * -1;
  }
  if(victim41Y >= windowHeight - 10 || victim41Y <= 10){
    victim41Yspeed = victim41Yspeed * -1;
  }

  victim42X = victim42X + victim42Xspeed;
  victim42Y = victim42Y + victim42Yspeed;
  if(victim42X >= windowWidth - 10 || victim42X <= 10){
    victim42Xspeed = victim42Xspeed * -1;
  }
  if(victim42Y >= windowHeight - 10 || victim42Y <= 10){
    victim42Yspeed = victim42Yspeed * -1;
  }

  victim43X = victim43X + victim43Xspeed;
  victim43Y = victim43Y + victim43Yspeed;
  if(victim43X >= windowWidth - 10 || victim43X <= 10){
    victim43Xspeed = victim43Xspeed * -1;
  }
  if(victim43Y >= windowHeight - 10 || victim43Y <= 10){
    victim43Yspeed = victim43Yspeed * -1;
  }

  victim44X = victim44X + victim44Xspeed;
  victim44Y = victim44Y + victim44Yspeed;
  if(victim44X >= windowWidth - 10 || victim44X <= 10){
    victim44Xspeed = victim44Xspeed * -1;
  }
  if(victim44Y >= windowHeight - 10 || victim44Y <= 10){
    victim44Yspeed = victim44Yspeed * -1;
  }

  victim45X = victim45X + victim45Xspeed;
  victim45Y = victim45Y + victim45Yspeed;
  if(victim45X >= windowWidth - 10 || victim45X <= 10){
    victim45Xspeed = victim45Xspeed * -1;
  }
  if(victim45Y >= windowHeight - 10 || victim45Y <= 10){
    victim45Yspeed = victim45Yspeed * -1;
  }

  victim46X = victim46X + victim46Xspeed;
  victim46Y = victim46Y + victim46Yspeed;
  if(victim46X >= windowWidth - 10 || victim46X <= 10){
    victim46Xspeed = victim46Xspeed * -1;
  }
  if(victim46Y >= windowHeight - 10 || victim46Y <= 10){
    victim46Yspeed = victim46Yspeed * -1;
  }

  victim47X = victim47X + victim47Xspeed;
  victim47Y = victim47Y + victim47Yspeed;
  if(victim47X >= windowWidth - 10 || victim47X <= 10){
    victim47Xspeed = victim47Xspeed * -1;
  }
  if(victim47Y >= windowHeight - 10 || victim47Y <= 10){
    victim47Yspeed = victim47Yspeed * -1;
  }

  victim48X = victim48X + victim48Xspeed;
  victim48Y = victim48Y + victim48Yspeed;
  if(victim48X >= windowWidth - 10 || victim48X <= 10){
    victim48Xspeed = victim48Xspeed * -1;
  }
  if(victim48Y >= windowHeight - 10 || victim48Y <= 10){
    victim48Yspeed = victim48Yspeed * -1;
  }

  victim49X = victim49X + victim49Xspeed;
  victim49Y = victim49Y + victim49Yspeed;
  if(victim49X >= windowWidth - 10 || victim49X <= 10){
    victim49Xspeed = victim49Xspeed * -1;
  }
  if(victim49Y >= windowHeight - 10 || victim49Y <= 10){
    victim49Yspeed = victim49Yspeed * -1;
  }

  victim50X = victim50X + victim50Xspeed;
  victim50Y = victim50Y + victim50Yspeed;
  if(victim50X >= windowWidth - 10 || victim50X <= 10){
    victim50Xspeed = victim50Xspeed * -1;
  }
  if(victim50Y >= windowHeight - 10 || victim50Y <= 10){
    victim50Yspeed = victim50Yspeed * -1;
  }


  //victim 1 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim1X, victim1Y)<20){
    victim1Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim1Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim1X, victim1Y, 20, 20);

  //victim 2 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim2X, victim2Y)<20){
    victim2Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim2Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim2X, victim2Y, 20, 20);

  //victim 3 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim3X, victim3Y)<20){
    victim3Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim3Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim3X, victim3Y, 20, 20);

  //victim 4 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim4X, victim4Y)<20){
    victim4Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim4Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim4X, victim4Y, 20, 20);

  //victim 5 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim5X, victim5Y)<20){
    victim5Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim5Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim5X, victim5Y, 20, 20);

  //victim 6 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim6X, victim6Y)<20){
    victim6Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim6Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim6X, victim6Y, 20, 20);

  //victim 7 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim7X, victim7Y)<20){
    victim7Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim7Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim7X, victim7Y, 20, 20);

  //victim 8 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim8X, victim8Y)<20){
    victim8Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim8Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim8X, victim8Y, 20, 20);

  //victim 9 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim9X, victim9Y)<20){
    victim9Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim9Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim9X, victim9Y, 20, 20);

  //victim 10 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim10X, victim10Y)<20){
    victim10Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim10Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim10X, victim10Y, 20, 20);

  //victim 11 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim11X, victim11Y)<20){
    victim11Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim11Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim11X, victim11Y, 20, 20);

  //victim 12 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim12X, victim12Y)<20){
    victim12Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim12Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim12X, victim12Y, 20, 20);

  //victim 13 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim13X, victim13Y)<20){
    victim13Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim13Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim13X, victim13Y, 20, 20);

  //victim 14 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim14X, victim14Y)<20){
    victim14Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim14Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim14X, victim14Y, 20, 20);

  //victim 15 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim15X, victim15Y)<20){
    victim15Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim15Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim15X, victim15Y, 20, 20);

  //victim 16 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim16X, victim16Y)<20){
    victim16Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim16Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim16X, victim16Y, 20, 20);

  //victim 17 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim17X, victim17Y)<20){
    victim17Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim17Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim17X, victim17Y, 20, 20);

  //victim 18 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim18X, victim18Y)<20){
    victim18Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim18Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim18X, victim18Y, 20, 20);

  //victim 19 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim19X, victim19Y)<20){
    victim19Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim19Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim19X, victim19Y, 20, 20);

  //victim 20 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim20X, victim20Y)<20){
    victim20Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim20Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim20X, victim20Y, 20, 20);

  //victim 21 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim21X, victim21Y)<20){
    victim21Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim21Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim21X, victim21Y, 20, 20);

  //victim 22 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim22X, victim22Y)<20){
    victim22Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim22Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim22X, victim22Y, 20, 20);

  //victim 23 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim23X, victim23Y)<20){
    victim23Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim23Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim23X, victim23Y, 20, 20);

  //victim 24 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim24X, victim24Y)<20){
    victim24Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim24Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim24X, victim24Y, 20, 20);

  //victim 25 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim25X, victim25Y)<20){
    victim25Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim25Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim25X, victim25Y, 20, 20);

  //victim 26 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim26X, victim26Y)<20){
    victim26Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim26Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim26X, victim26Y, 20, 20);

  //victim 27 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim27X, victim27Y)<20){
    victim27Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim27Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim27X, victim27Y, 20, 20);

  //victim 28 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim28X, victim28Y)<20){
    victim28Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim28Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim28X, victim28Y, 20, 20);

  //victim 29 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim29X, victim29Y)<20){
    victim29Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim29Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim29X, victim29Y, 20, 20);

  //victim 30 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim30X, victim30Y)<20){
    victim30Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim30Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim30X, victim30Y, 20, 20);

  //victim 31 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim31X, victim31Y)<20){
    victim31Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim31Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim31X, victim31Y, 20, 20);

  //victim 32 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim32X, victim32Y)<20){
    victim32Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim32Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim32X, victim32Y, 20, 20);

  //victim 33 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim33X, victim33Y)<20){
    victim33Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim33Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim33X, victim33Y, 20, 20);

  //victim 34 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim34X, victim34Y)<20){
    victim34Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim34Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim34X, victim34Y, 20, 20);

  //victim 35 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim35X, victim35Y)<20){
    victim35Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim35Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim35X, victim35Y, 20, 20);

  //victim 36 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim36X, victim36Y)<20){
    victim36Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim36Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim36X, victim36Y, 20, 20);

  //victim 37 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim37X, victim37Y)<20){
    victim37Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim37Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim37X, victim37Y, 20, 20);

  //victim 38 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim38X, victim38Y)<20){
    victim38Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim38Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim38X, victim38Y, 20, 20);

  //victim 39 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim39X, victim39Y)<20){
    victim39Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim39Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim39X, victim39Y, 20, 20);

  //victim 40 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim40X, victim40Y)<20){
    victim40Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim40Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim40X, victim40Y, 20, 20);

  //victim 41 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim41X, victim41Y)<20){
    victim41Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim41Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim41X, victim41Y, 20, 20);

  //victim 42 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim42X, victim42Y)<20){
    victim42Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim42Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim42X, victim42Y, 20, 20);

  //victim 43 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim43X, victim43Y)<20){
    victim43Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim43Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim43X, victim43Y, 20, 20);

  //victim 44 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim44X, victim44Y)<20){
    victim44Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim44Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim44X, victim44Y, 20, 20);

  //victim 45 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim45X, victim45Y)<20){
    victim45Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim45Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim45X, victim45Y, 20, 20);

  //victim 46 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim46X, victim46Y)<20){
    victim46Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim46Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim46X, victim46Y, 20, 20);

  //victim 47 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim47X, victim47Y)<20){
    victim47Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim47Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim47X, victim47Y, 20, 20);

  //victim 48 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim48X, victim48Y)<20){
    victim48Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim48Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim48X, victim48Y, 20, 20);

  //victim 49 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim49X, victim49Y)<20){
    victim49Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim49Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim49X, victim49Y, 20, 20);

  //victim 50 info
  //if the virus gets to close, the boolean flips to true
  if(dist(virusX, virusY, victim50X, victim50Y)<20){
    victim50Bool = true;
  }
  //if boolean is true, the victim is infected so the color changes to red
  if(victim50Bool == true){
    fill(255, 0, 0);
  }else{
    fill(255);
  }
  ellipse(victim50X, victim50Y, 20, 20);
}


//"heal" the victims with mouse press
function mousePressed(){
  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim1X, victim1Y) < 20 && victim1Bool==true){
    victim1Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim2X, victim2Y)< 20 && victim2Bool==true){
    victim2Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim3X, victim3Y) < 20 && victim3Bool==true){
    victim3Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim4X, victim4Y)< 20 && victim4Bool==true){
    victim4Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim5X, victim5Y) < 20 && victim5Bool==true){
    victim5Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim6X, victim6Y)< 20 && victim6Bool==true){
    victim6Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim7X, victim7Y) < 20 && victim7Bool==true){
    victim7Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim8X, victim8Y)< 20 && victim8Bool==true){
    victim8Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim9X, victim9Y)< 20 && victim9Bool==true){
    victim9Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim10X, victim10Y)< 20 && victim10Bool==true){
    victim10Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim11X, victim11Y) < 20 && victim11Bool==true){
    victim11Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim12X, victim12Y)< 20 && victim12Bool==true){
    victim12Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim13X, victim13Y) < 20 && victim13Bool==true){
    victim13Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim14X, victim14Y)< 20 && victim14Bool==true){
    victim14Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim15X, victim15Y) < 20 && victim15Bool==true){
    victim15Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim16X, victim16Y)< 20 && victim16Bool==true){
    victim16Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim17X, victim17Y) < 20 && victim17Bool==true){
    victim17Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim18X, victim18Y)< 20 && victim18Bool==true){
    victim18Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim19X, victim19Y)< 20 && victim19Bool==true){
    victim19Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim20X, victim20Y)< 20 && victim20Bool==true){
    victim20Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim21X, victim21Y) < 20 && victim21Bool==true){
    victim21Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim22X, victim22Y)< 20 && victim22Bool==true){
    victim22Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim23X, victim23Y) < 20 && victim23Bool==true){
    victim23Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim24X, victim24Y)< 20 && victim24Bool==true){
    victim24Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim25X, victim25Y) < 20 && victim25Bool==true){
    victim25Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim26X, victim26Y)< 20 && victim26Bool==true){
    victim26Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim27X, victim27Y) < 20 && victim27Bool==true){
    victim27Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim28X, victim28Y)< 20 && victim28Bool==true){
    victim28Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim29X, victim29Y)< 20 && victim29Bool==true){
    victim29Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim30X, victim30Y)< 20 && victim30Bool==true){
    victim30Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim31X, victim31Y) < 20 && victim31Bool==true){
    victim31Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim32X, victim32Y)< 20 && victim32Bool==true){
    victim32Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim33X, victim33Y) < 20 && victim33Bool==true){
    victim33Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim34X, victim34Y)< 20 && victim34Bool==true){
    victim34Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim35X, victim35Y) < 20 && victim35Bool==true){
    victim35Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim36X, victim36Y)< 20 && victim36Bool==true){
    victim36Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim37X, victim37Y) < 20 && victim37Bool==true){
    victim37Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim38X, victim38Y)< 20 && victim38Bool==true){
    victim38Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim39X, victim39Y)< 20 && victim39Bool==true){
    victim39Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim40X, victim40Y)< 20 && victim40Bool==true){
    victim40Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim41X, victim41Y) < 20 && victim41Bool==true){
    victim41Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim42X, victim42Y)< 20 && victim42Bool==true){
    victim42Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim43X, victim43Y) < 20 && victim43Bool==true){
    victim43Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim44X, victim44Y)< 20 && victim44Bool==true){
    victim44Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim45X, victim45Y) < 20 && victim45Bool==true){
    victim45Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim46X, victim46Y)< 20 && victim46Bool==true){
    victim46Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim47X, victim47Y) < 20 && victim47Bool==true){
    victim47Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim48X, victim48Y)< 20 && victim48Bool==true){
    victim48Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim49X, victim49Y)< 20 && victim49Bool==true){
    victim49Bool = false;
  }

  //flip the boolean back to false and heal them
  if(dist(mouseX, mouseY, victim50X, victim50Y)< 20 && victim50Bool==true){
    victim50Bool = false;
  }
}


function windowResized(){
  resizeCanvas(windowWidth, windowHeight);
}
